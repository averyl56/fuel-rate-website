import React from 'react';
// you can use this to format the form
import '../css/formpage.css';

// page that allows a user to calculate fuel rates

// Full Name (50 chars, required)
// Address 1 (100 chars, required)
// Address 2 (100 chars, optional)
// City (100 chars, required)
// State (Drop down, selection required) DB stores 2 char state code
// Zipcode (9 chars, at least 5 requried)

function FuelRate() {
    return (<div>Fuel Rate</div>);
}

export default FuelRate;